<UserControl x:Class="InventorySystem.UI.Views.POSView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="350"/>
        </Grid.ColumnDefinitions>

        <ScrollViewer Grid.Column="0" Margin="0,0,20,0">
            <ItemsControl ItemsSource="{Binding AvailableProducts}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Command="{Binding DataContext.AddToCartCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                CommandParameter="{Binding}"
                                Width="140" Height="100" Margin="0,0,15,15"
                                Background="White" BorderBrush="#E0E0E0" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14" 
                                           TextWrapping="Wrap" TextAlignment="Center" Foreground="#333"/>
                                <TextBlock Text="{Binding SellingPrice, StringFormat={}{0:N2}}" 
                                           TextAlignment="Center" Foreground="#009688" Margin="0,5,0,0"/>
                                <TextBlock Text="{Binding Quantity, StringFormat={}Stock: {0}}" 
                                           TextAlignment="Center" FontSize="11" Foreground="Gray"/>
                            </StackPanel>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Border Grid.Column="1" Background="White" BorderBrush="#E0E0E0" BorderThickness="1,0,0,0">
            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Current Bill" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

                <ScrollViewer Grid.Row="1">
                    <ItemsControl ItemsSource="{Binding Cart}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{Binding Product.Name}" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding Product.SellingPrice, StringFormat={}{0:N2} each}" FontSize="11" Foreground="Gray"/>
                                    </StackPanel>

                                    <TextBlock Grid.Column="1" Text="{Binding Quantity, StringFormat={}x{0}}" 
                                               VerticalAlignment="Center" Margin="10,0" FontWeight="Bold"/>

                                    <TextBlock Grid.Column="2" Text="{Binding TotalPrice, StringFormat={}{0:N2}}" 
                                               VerticalAlignment="Center" Foreground="#009688"/>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <StackPanel Grid.Row="2" Margin="0,20,0,0">
                    <Separator Margin="0,0,0,15"/>
                    <DockPanel LastChildFill="False">
                        <TextBlock Text="Total:" FontSize="18" FontWeight="Bold"/>
                        <TextBlock Text="{Binding GrandTotal, StringFormat={}{0:N2}}" 
                                   DockPanel.Dock="Right" FontSize="20" FontWeight="Bold" Foreground="#009688"/>
                    </DockPanel>

                    <Button Content="PAY &amp; PRINT" Command="{Binding CheckoutCommand}" 
                            Height="50" FontSize="16" FontWeight="Bold" Margin="0,20,0,0"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>