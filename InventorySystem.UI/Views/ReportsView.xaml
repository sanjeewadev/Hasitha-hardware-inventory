<UserControl x:Class="InventorySystem.UI.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Analytics Overview" FontSize="22" FontWeight="Bold" Margin="0,0,0,20" Foreground="#2B3649"/>

        <Grid Grid.Row="1" Margin="0,0,0,25">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#009688" CornerRadius="8" Padding="20" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="Total Sales" Foreground="White" Opacity="0.8" FontSize="14"/>
                    <TextBlock Text="{Binding TotalSales, StringFormat={}{0:C}}" 
                               Foreground="White" FontWeight="Bold" FontSize="28" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Background="#2980B9" CornerRadius="8" Padding="20" Margin="10,0">
                <StackPanel>
                    <TextBlock Text="Total Profit" Foreground="White" Opacity="0.8" FontSize="14"/>
                    <TextBlock Text="{Binding TotalProfit, StringFormat={}{0:C}}" 
                               Foreground="White" FontWeight="Bold" FontSize="28" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="#8E44AD" CornerRadius="8" Padding="20" Margin="10,0,0,0">
                <StackPanel>
                    <TextBlock Text="Items Sold" Foreground="White" Opacity="0.8" FontSize="14"/>
                    <TextBlock Text="{Binding TotalItemsSold}" 
                               Foreground="White" FontWeight="Bold" FontSize="28" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" CornerRadius="5" BorderBrush="#E0E0E0" BorderThickness="1" Margin="0,0,15,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Recent Transactions" FontSize="16" FontWeight="SemiBold" Margin="20" Foreground="#555"/>

                    <DataGrid Grid.Row="1" ItemsSource="{Binding RecentSales}" AutoGenerateColumns="False" 
                              IsReadOnly="True" BorderThickness="0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat=dd MMM HH:mm}" Width="120"/>
                            <DataGridTextColumn Header="Product" Binding="{Binding Product.Name}" Width="*"/>
                            <DataGridTextColumn Header="Qty" Binding="{Binding Quantity}" Width="80"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <Border Grid.Column="1" Background="White" CornerRadius="5" BorderBrush="#E74C3C" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Margin="20">
                        <TextBlock Text="⚠️ Low Stock Alert" FontSize="16" FontWeight="SemiBold" Foreground="#E74C3C"/>
                    </StackPanel>

                    <DataGrid Grid.Row="1" ItemsSource="{Binding LowStockItems}" AutoGenerateColumns="False" 
                              IsReadOnly="True" BorderThickness="0" HeadersVisibility="Column">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Product" Binding="{Binding Name}" Width="*"/>
                            <DataGridTextColumn Header="Left" Binding="{Binding Quantity}" Width="60" FontWeight="Bold"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>